user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
                    
    access_log /var/log/nginx/access.log main;
    
    sendfile on;
    keepalive_timeout 65;

    # Dynamic upstream mapping
    upstream dashboard_server {
        server dashboard:3000;
    }

    upstream superlink_server {
        server superlink:8080;
    }

    server {
        listen 80;
        server_name localhost;

        # Redirect to HTTPS placeholder (if SSL certificates are provided)
        # return 301 https://$host$request_uri;

        # Next.js Dashboard Reverse Proxy
        location / {
            proxy_pass http://dashboard_server;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Flower gRPC and REST APIs Proxy
        location /api/federation/ {
            proxy_pass http://superlink_server;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
